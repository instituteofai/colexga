// import { selectMaintainTaskDomain } from '../selectors';

describe('selectMaintainTaskDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
