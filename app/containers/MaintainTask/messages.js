/*
 * MaintainTask Messages
 *
 * This contains all the text for the MaintainTask container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MaintainTask';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MaintainTask container!',
  },
});
