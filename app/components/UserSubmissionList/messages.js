/*
 * UserSubmissionList Messages
 *
 * This contains all the text for the UserSubmissionList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UserSubmissionList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserSubmissionList component!',
  },
});
