/*
 * Timer Messages
 *
 * This contains all the text for the Timer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Timer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Time left',
  },
});
